import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    margin: "100px auto",
    maxWidth: "500px",
    display: `flex`,
    flexDirection: `row`,
    justifyContent: `center`,
    alignItems: "center",
  },
});
